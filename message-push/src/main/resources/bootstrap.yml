# Tomcat
server: 
  port: 9217

# Spring
spring: 
  application:
    # 应用名称
    name: message-push
  main:
    #允许存在多个Feign调用相同Service的接口
    allow-bean-definition-overriding: true
  profiles:
    # 环境配置
    active: dev


